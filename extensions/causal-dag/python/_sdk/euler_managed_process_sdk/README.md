# Vendored: euler_managed_process_sdk

`server.py` and `__init__.py` here are copied **verbatim** from
`euler@7b0dc7c` `python/euler_managed_process_sdk/src/euler_managed_process_sdk`.
They are the whole managed-process JSON-RPC stdio client (`serve`,
`CommandContext`, `Host`, `ProtocolError`/`HostError`/`Cancelled`).

Do not edit them here. This vendoring is a stop-gap until the SDK is published
as an installable package (euler-extensions#1); when that lands, this directory
is deleted and the dependency is declared instead. Any change belongs upstream.

To refresh: re-copy the two files from the pinned commit and update the hash
above — never patch the local copy.
